import { LightningElement } from 'lwc';

export default class GalleryPage extends LightningElement {
    // This component is a static gallery showcase
    // No dynamic behavior required at this time
}
